import './App.css';
import MovieList from './components/MovieList';

// in this lab we will see how to maintain information in html form tags, the component state helps us keep date in a form

function App() {
  return (
    <div className="App">
      <MovieList />
    </div>
  );
}

export default App;
